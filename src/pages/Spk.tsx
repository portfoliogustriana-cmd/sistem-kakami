import React, { useState, useEffect } from "react";
import { useLocation } from "react-router-dom";
import {
  Calendar,
  Layers,
  Image,
  Check,
  ListPlus,
  ShieldAlert,
  ArrowRight,
  Eye,
  RefreshCw,
  X,
  PlusSquare,
  Trash2,
  Printer,
  Search,
  Copy,
} from "lucide-react";
import {
  getStoredData,
  saveStoredData,
  Order,
  OrderItem,
  getStoredMonth,
  setStoredMonth,
} from "../lib/storage";
import { SpkPrintDocument } from "../components/SpkPrintDocument";
const Spk = () => {
  const location = useLocation();
  const [orders, setOrders] = useState<Order[]>([]);
  const [orderItems, setOrderItems] = useState<OrderItem[]>([]);
  const [bulan, setBulanState] = useState(getStoredMonth());
  const setBulan = (val: string) => {
    setBulanState(val);
    setStoredMonth(val);
  };
  const [searchSpk, setSearchSpk] = useState("");
  const [filterSpk, setFilterSpk] = useState("SEMUA"); 
  const [statusFilter, setStatusFilter] = useState("SEMUA"); // "SEMUA" | "AKTIF" | "SELESAI" | "LENGKAP" | "KURANG"
/* SPK Editing States */
 const [selectedOrderId, setSelectedOrderId] = useState<number | null>(null); 
/* Form Fields */
 const [bahanUtama, setBahanUtama] = useState(''); const [bentukKerah, setBentukKerah] = useState(''); const [jenisOrderan, setJenisOrderan] = useState('Full Set'); const [bahanCelana, setBahanCelana] = useState(''); const [bahanKerah, setBahanKerah] = useState(''); const [bahanManset, setBahanManset] = useState(''); const [polaBaju, setPolaBaju] = useState(''); const [catatanDarurat, setCatatanDarurat] = useState(''); const [catatanProduksi, setCatatanProduksi] = useState(''); const [deadline, setDeadline] = useState(''); const [fotoMockup, setFotoMockup] = useState(''); 
/* Live Jersey Sizing Rows */
 const [sizingRows, setSizingRows] = useState<Omit<OrderItem, 'id' | 'order_id'>[]>([]); const [playerSearch, setPlayerSearch] = useState(''); const [hasDuplicateNumbers, setHasDuplicateNumbers] = useState(false); 
/* Print Preview */
 const [showSpkPrintObj, setShowSpkPrintObj] = useState<Order | null>(null); const sizeOptions = [ 'XXS ANAK', 'XS ANAK', 'S ANAK', 'M ANAK', 'L ANAK', 'XL ANAK', '2XL ANAK', 'XS', 'S', 'M', 'L', 'XL', 'XXL', '3XL', '4XL', '5XL', 'CUSTOM' ]; useEffect(() => { const data = getStoredData(); setOrders(data.orders); setOrderItems(data.orderItems); 
/* Deep link detection */
 const queryParams = new URLSearchParams(location.search); const orderIdParam = queryParams.get('orderId'); if (orderIdParam) { const orderIdInt = parseInt(orderIdParam); if (!isNaN(orderIdInt)) { setSelectedOrderId(orderIdInt); const target = data.orders.find((o) => o.id === orderIdInt); if (target) { loadSPKFieldsOfOrder(target, data.orderItems.filter((i) => i.order_id === orderIdInt)); } } } else if (data.orders.length > 0) { 
/* Pre-select first order matching active month by default */
 const filtered = data.orders.filter((o) => o.tanggal_order.startsWith(bulan)); if (filtered.length > 0) { setSelectedOrderId(filtered[0].id); loadSPKFieldsOfOrder(filtered[0], data.orderItems.filter((i) => i.order_id === filtered[0].id)); } } }, [location.search]); 
/* Handle month selection shift */
 const handleMonthChange = (val: string) => { setBulan(val); const data = getStoredData(); const filtered = data.orders.filter((o) => o.tanggal_order.startsWith(val)); if (filtered.length > 0) { setSelectedOrderId(filtered[0].id); loadSPKFieldsOfOrder(filtered[0], data.orderItems.filter((i) => i.order_id === filtered[0].id)); } else { setSelectedOrderId(null); clearFields(); } }; const clearFields = () => { setBahanUtama(''); setBentukKerah(''); setJenisOrderan('Full Set'); setBahanCelana(''); setBahanKerah(''); setBahanManset(''); setPolaBaju(''); setCatatanDarurat(''); setCatatanProduksi(''); setDeadline(''); setFotoMockup(''); setSizingRows([]); }; const loadSPKFieldsOfOrder = (o: Order, items: OrderItem[]) => { setBahanUtama(o.bahan_utama || ''); setBentukKerah(o.bentuk_kerah || ''); setJenisOrderan(o.jenis_orderan || 'Full Set'); setBahanCelana(o.bahan_celana || ''); setBahanKerah(o.bahan_kerah || ''); setBahanManset(o.bahan_manset || ''); setPolaBaju(o.pola_baju || ''); setCatatanDarurat(o.catatan_darurat || ''); setCatatanProduksi(o.catatan_produksi_spk || ''); setDeadline(o.deadline_kerja || o.tanggal_order || ''); setFotoMockup(o.foto_mockup || ''); const rows = items.map((i) => ({ no_player: i.no_player, nama_player: i.nama_player, nama_punggung: i.nama_punggung, size: i.size, lengan: i.lengan, gender: i.gender, keterangan: i.keterangan, })); setSizingRows(rows); checkDuplicateNumbers(rows); }; 
/* Check */
/* if jersey / numbers are duplicate */
 const checkDuplicateNumbers = (rows: Omit<OrderItem, 'id' | 'order_id'>[]) => { const numbers = rows.map(r => r.no_player.trim()).filter(n => n !== '' && n !== '-'); const duplicates = numbers.filter((item, index) => numbers.indexOf(item) !== index); setHasDuplicateNumbers(duplicates.length > 0); }; const handleSizingFieldChange = (idx: number, field: string, val: string) => { const updated = sizingRows.map((row, i) => { if (i === idx) { return { ...row, [field]: val }; } return row; }); setSizingRows(updated); checkDuplicateNumbers(updated); }; const handleAddRow = () => { const updated = [ ...sizingRows, { no_player: '', nama_player: '', nama_punggung: '', size: 'L', lengan: 'PENDEK', gender: 'PRIA', keterangan: '', }, ]; setSizingRows(updated); checkDuplicateNumbers(updated); }; const handleRemoveRow = (idx: number) => { const updated = sizingRows.filter((_, i) => i !== idx); setSizingRows(updated); checkDuplicateNumbers(updated); }; 
/* Populate spec values from another previous project (Auto fill specs feature) */
 const handleAutoFillCopy = (prevOrderJson: string) => { if (!prevOrderJson) return; try { const parsed: Order = JSON.parse(prevOrderJson); setBahanUtama(parsed.bahan_utama || ''); setBentukKerah(parsed.bentuk_kerah || ''); setJenisOrderan(parsed.jenis_orderan || 'Full Set'); setBahanCelana(parsed.bahan_celana || ''); setBahanKerah(parsed.bahan_kerah || ''); setBahanManset(parsed.bahan_manset || ''); setPolaBaju(parsed.pola_baju || ''); setCatatanProduksi(parsed.catatan_produksi_spk || ''); alert(`Berhasil menyalin spesifikasi dari order ${parsed.invoice_no}!`); } catch (e) { console.error(e); } }; const handleFileChange = (e: React.ChangeEvent<HTMLInputElement>) => { const file = e.target.files?.[0]; if (file) { const reader = new FileReader(); reader.onloadend = () => { setFotoMockup(reader.result as string); }; reader.readAsDataURL(file); } }; 
/* Save changes with size weights sorting model */
 const handleSaveSPK = (e: React.FormEvent) => { e.preventDefault(); if (!selectedOrderId) { alert('Pilih order terlebih dahulu!'); return; } const data = getStoredData(); 
/* Map size weight scores for precise sorting */
 const sizeWeights: { [key: string]: number } = { 'XXS ANAK': 1, 'XS ANAK': 2, 'S ANAK': 3, 'M ANAK': 4, 'L ANAK': 5, 'XL ANAK': 6, '2XL ANAK': 7, 'XS': 8, 'S': 9, 'M': 10, 'L': 11, 'XL': 12, 'XXL': 13, '3XL': 14, '4XL': 15, '5XL': 16, 'CUSTOM': 99 }; 
/* Prepare sorted list items */
 const preparedItems = sizingRows.map((it, idx) => { const szUpper = (it.size || '').toUpperCase().trim(); const weight = sizeWeights[szUpper] ?? 90; return { ...it, weight, }; }); 
/* Sort items based on sizing weights preparedItems.sort((a, b) => a.weight - b.weight);  */

/* Save items to local database */
 const mappedItems: OrderItem[] = preparedItems.map((it, index) => ({ id: Date.now() + index, order_id: selectedOrderId, no_player: it.no_player, nama_player: it.nama_player, nama_punggung: it.nama_punggung, size: it.size, lengan: it.lengan, gender: it.gender, keterangan: it.keterangan, })); 
/* Update order spec attributes */
 const updatedOrders = data.orders.map((o) => { if (o.id === selectedOrderId) { return { ...o, bahan_utama: bahanUtama, bentuk_kerah: bentukKerah, jenis_orderan: jenisOrderan, bahan_celana: bahanCelana, bahan_kerah: bahanKerah, bahan_manset: bahanManset, pola_baju: polaBaju, catatan_darurat: catatanDarurat, catatan_produksi_spk: catatanProduksi, dibuat_oleh: 'Admin ERP', deadline_kerja: deadline, foto_mockup: fotoMockup, }; } return o; }); 
/* Clean old items for this order and insert sorted items */
 const remainingItems = data.orderItems.filter((i) => i.order_id !== selectedOrderId); const finalItems = [...remainingItems, ...mappedItems]; setOrders(updatedOrders); setOrderItems(finalItems); saveStoredData(updatedOrders, finalItems, data.transactions); alert('Spesifikasi & list ukuran SPK berhasil disimpan dan diurutkan berdasarkan standard sizing!'); }; const handleOpenPrintObj = (order: Order) => { setShowSpkPrintObj(order); }; 
/* Filtering lists */
  const filteredOrdersList = orders
    .filter((o) => o.tanggal_order.startsWith(bulan))
    .filter(
      (o) =>
        (o.nama_konsumen || "").toLowerCase().includes(searchSpk.toLowerCase()) ||
        (o.nama_order || "").toLowerCase().includes(searchSpk.toLowerCase()) ||
        (o.invoice_no || "").toLowerCase().includes(searchSpk.toLowerCase())
    )
    .filter((o) => filterSpk === "SEMUA" || o.jenis_produk === filterSpk)
    .filter((o) => {
      if (statusFilter === "SEMUA") return true;
      if (statusFilter === "AKTIF") return o.status_tracking !== "Selesai";
      if (statusFilter === "SELESAI") return o.status_tracking === "Selesai";
      
      const terinput = orderItems.filter((i) => i.order_id === o.id).length;
      const isComplete = terinput >= o.jumlah_pcs;
      if (statusFilter === "LENGKAP") return isComplete;
      if (statusFilter === "KURANG") return !isComplete;
      
      return true;
    });
  const activeOrder = orders.find((o) => o.id === selectedOrderId); 
/* Search filter inside sizing list rows */
 const displayedSizingRows = sizingRows.filter( (row) => (row.nama_player || '').toLowerCase().includes(playerSearch.toLowerCase()) || (row.nama_punggung || '').toLowerCase().includes(playerSearch.toLowerCase()) || (row.no_player || '').includes(playerSearch) ); return ( <div className="p-4 sm:p-8 w-full max-w-7xl mx-auto flex flex-col gap-6"> {/* HEADER PANEL */} <div className="flex flex-col sm:flex-row justify-between items-start sm:items-center bg-white/60 backdrop-blur-md p-6 rounded-3xl shadow-sm border border-black"> <div> <h2 className="text-2xl font-black bg-clip-text text-transparent bg-gradient-to-r from-black to-black tracking-tight flex items-center gap-2"> <Layers className="w-6 h-6 text-black" /> SPK Produksi & Sizing </h2> <p className="text-xs text-black font-medium mt-1"> Persiapkan Surat Perintah Kerja (SPK), isikan detail ukuran jersey pemain dengan pendeteksi nomor punggung ganda. </p> </div> <form onSubmit={(e) => e.preventDefault()} className="mt-3 sm:mt-0 flex items-center gap-2 bg-white p-1.5 rounded-2xl border border-black shadow-inner"> <label className="font-extrabold text-[10px] text-black pl-2 uppercase tracking-wider">Pilih Bulan:</label> <input type="month" value={bulan} onChange={(e) => handleMonthChange(e.target.value)} className="bg-white hover:bg-white px-3 py-1.5 rounded-xl border-none outline-none text-xs font-bold text-black cursor-pointer transition-colors" /> </form> </div> {activeOrder && ( <div id="spk-form-box" className="bg-white p-6 sm:p-8 rounded-3xl shadow-xl border border-black space-y-6 relative overflow-hidden"> <div className="flex flex-col sm:flex-row justify-between items-start sm:items-center border-b pb-4 border-black gap-3"> <div> <h2 className="text-lg font-black text-black uppercase tracking-tight flex items-center gap-2"> 📂 Manajemen SPK: <span className="text-black">{activeOrder.nama_order}</span> </h2> <div className="flex items-center gap-2 mt-1 flex-wrap"> <span className="text-[10px] font-black text-black bg-white px-2 py-0.5 rounded-md uppercase tracking-wider"> {activeOrder.invoice_no} </span> <span className="text-[10px] font-black text-black bg-white px-2 py-0.5 rounded-md uppercase tracking-wider"> {activeOrder.jenis_produk} </span> <span className="text-[10px] text-black font-bold"> Sizing Target: <strong className="text-black">{activeOrder.jumlah_pcs} Pcs</strong> </span> </div> </div> <button onClick={() => setSelectedOrderId(null)} className="text-[10px] bg-white hover:bg-black/10 text-black px-4 py-2 rounded-xl font-black uppercase tracking-wider flex items-center gap-1 transition-all" > <X size={12} /> Tutup Form </button> </div> {/* COPY FROM PRIOR ORDERS DROPDOWN */} <div className="bg-white/50 border border-black p-4 rounded-2xl flex flex-col sm:flex-row items-center justify-between gap-3 shadow-inner"> <span className="text-[10px] font-black text-black tracking-wider uppercase flex items-center gap-1.5"> <Copy size={13} className="text-black" /> Salin Spesifikasi Dari Project Lama: </span> <select onChange={(e) => handleAutoFillCopy(e.target.value)} className="w-full sm:w-auto border border-black pr-8 pl-3 py-1.5 text-xs rounded-xl bg-white font-bold text-black focus:ring-2 focus:ring-black outline-none cursor-pointer" > <option value="">-- Pilih Project Sebelumnya --</option> {orders .filter((o) => o.id !== activeOrder.id && (o.bahan_utama || o.bentuk_kerah)) .slice(0, 20) .map((hq) => ( <option key={hq.id} value={JSON.stringify(hq)}> {hq.invoice_no} - {hq.nama_order} </option> ))} </select> </div> <form onSubmit={handleSaveSPK} className="space-y-6"> <div className="bg-white rounded-2xl border border-black space-y-4"> <h3 className="text-[10px] font-extrabold uppercase tracking-widest text-black border-b border-black pb-2"> 📋 Spesifikasi Material & Waktu Pekerjaan </h3> <div className="grid grid-cols-1 md:grid-cols-4 gap-4"> <div> <label className="block text-[9px] font-black text-black mb-1 uppercase">Bahan Utama Baju:</label> <input type="text" value={bahanUtama} onChange={(e) => setBahanUtama(e.target.value)} placeholder="Jacquart / Drifit Milenial" className="w-full bg-white border border-black p-2.5 rounded-xl outline-none font-bold text-xs text-black" required /> </div> <div> <label className="block text-[9px] font-black text-black mb-1 uppercase">Bentuk Kerah:</label> <input type="text" value={bentukKerah} onChange={(e) => setBentukKerah(e.target.value)} placeholder="V-Neck / O-Neck Tumpuk" className="w-full bg-white border border-black p-2.5 rounded-xl outline-none font-bold text-xs text-black" required /> </div> <div> <label className="block text-[9px] font-black text-black mb-1 uppercase">Bahan Celana:</label> <input type="text" value={bahanCelana} onChange={(e) => setBahanCelana(e.target.value)} placeholder="Lotto / Drifit Jarum" className="w-full bg-white border border-black p-2.5 rounded-xl outline-none font-bold text-xs text-black" /> </div> <div> <label className="block text-[9px] font-black text-black mb-1 uppercase">Bahan Kerah (RIB):</label> <input type="text" value={bahanKerah} onChange={(e) => setBahanKerah(e.target.value)} placeholder="Rib Premium" className="w-full bg-white border border-black p-2.5 rounded-xl outline-none font-bold text-xs text-black" /> </div> </div> <div className="grid grid-cols-1 md:grid-cols-4 gap-4"> <div> <label className="block text-[9px] font-black text-black mb-1 uppercase">Bahan Manset:</label> <input type="text" value={bahanManset} onChange={(e) => setBahanManset(e.target.value)} placeholder="Spandek Premium" className="w-full bg-white border border-black p-2.5 rounded-xl outline-none font-bold text-xs text-black" /> </div> <div> <label className="block text-[9px] font-black text-black mb-1 uppercase">Pola / Potongan Baju:</label> <input type="text" value={polaBaju} onChange={(e) => setPolaBaju(e.target.value)} placeholder="Pola Standar / Raglan / Set-In" className="w-full bg-white border border-black p-2.5 rounded-xl outline-none font-bold text-xs text-black" /> </div> <div> <label className="block text-[9px] font-black text-black mb-1 uppercase">Jenis Orderan:</label> <select value={jenisOrderan} onChange={(e) => setJenisOrderan(e.target.value)} className="w-full bg-white border border-black p-2.5 rounded-xl outline-none font-bold text-xs text-black cursor-pointer text-left" > <option value="Full Set">Full Set (Atasan + Celana)</option> <option value="Atasan Saja">Atasan Saja</option> <option value="Celana Saja">Celana Saja</option> </select> </div> <div> <label className="block text-[9px] font-black text-black mb-1 uppercase">Deadline Selesai SPK:</label> <input type="date" value={deadline} onChange={(e) => setDeadline(e.target.value)} className="w-full bg-white/50 border border-black p-2.5 rounded-xl outline-none font-extrabold text-xs text-black cursor-pointer" required /> </div> </div> <div className="grid grid-cols-1 md:grid-cols-3 gap-4 pt-2"> <div> <label className="block text-[9px] font-black text-black mb-1 uppercase">Simulasi File Mockup Desain (URL atau Unggah):</label> <div className="flex gap-2"> <input type="text" value={fotoMockup} onChange={(e) => setFotoMockup(e.target.value)} placeholder="https://images.unsplash.com/..." className="flex-grow bg-white border border-black p-2.5 rounded-xl outline-none font-semibold text-xs text-black" /> <label className="cursor-pointer bg-white hover:bg-black/10 text-black p-2.5 rounded-xl text-xs font-bold flex items-center"> <Image size={16} /> <input type="file" accept="image/*" className="hidden" onChange={(e) => { if (e.target.files && e.target.files[0]) { const reader = new FileReader(); reader.onload = (event) => { if (event.target && typeof event.target.result === 'string') { setFotoMockup(event.target.result); } }; reader.readAsDataURL(e.target.files[0]); } }} /> </label> </div> </div> <div> <label className="block text-[9px] font-black text-black mb-1 uppercase">Catatan Produksi:</label> <textarea rows={2} value={catatanProduksi} onChange={(e) => setCatatanProduksi(e.target.value)} placeholder="- Detail cetakan tajam&#10;- Bebas noda tinta" className="w-full bg-white border border-black p-2.5 rounded-xl outline-none font-medium text-xs text-black resize-none" /> </div> <div> <label className="block text-[9px] font-black text-black mb-1 uppercase">Catatan Darurat / Revisi:</label> <textarea rows={2} value={catatanDarurat} onChange={(e) => setCatatanDarurat(e.target.value)} placeholder="- REVISI kerah nomor 10 jadi V-neck tumpuk." className="w-full bg-white/50 border border-black p-2.5 rounded-xl outline-none font-semibold text-xs text-black resize-none" /> </div> </div> {fotoMockup && ( <div className="pt-2"> <span className="block text-[9px] font-black text-black uppercase tracking-widest mb-1.5">Gambar Mockup Yang Terintegrasi:</span> <div className="flex gap-4 items-center"> <img src={fotoMockup} alt="mockup target" className="w-20 h-20 object-cover rounded-xl border border-black bg-white" onError={(e) => { (e.target as HTMLImageElement).src = 'https://images.unsplash.com/photo-1541252260730-0412e8e2108e?w=100'; }} /> <div className="text-[10px] text-black font-bold">Simulator Mockup berhasil dipasang. SPK akan mencetak dengan mockup ini.</div> </div> </div> )} </div> <div className="space-y-4"> <div className="flex flex-col md:flex-row justify-between items-start md:items-end gap-3 border-b border-black pb-3"> <div> <h3 className="text-xs font-black uppercase tracking-widest text-black flex items-center gap-2"> 👕 Rincian Atribut Pemain & Standard Sizing Baju </h3> </div> <div className="flex items-center gap-3 w-full md:w-auto"> <div className="relative flex-grow md:flex-none"> <Search className="w-4 h-4 text-black absolute left-3.5 top-2.5" /> <input type="text" value={playerSearch} onChange={(e) => setPlayerSearch(e.target.value)} placeholder="Cari No / Nama..." className="w-full md:w-48 pl-9 bg-white border border-black p-2 rounded-xl text-xs font-bold focus:outline-none" /> </div> <button type="button" onClick={handleAddRow} className="bg-black hover:bg-gray-800 text-white px-4 py-2 rounded-xl font-black text-[10px] uppercase tracking-wider flex items-center gap-1 shadow-md" > <ListPlus size={14} /> Tambah Baris </button> </div> </div> {/* DUPLICATE WARNING */} {hasDuplicateNumbers && ( <div className="bg-gradient-to-r from-black/10 to-black/10 text-black text-xs p-3.5 rounded-2xl border border-black flex items-center gap-3.5 font-bold animate-pulse"> <ShieldAlert className="w-5 h-5 text-black animate-bounce" /> <div> <span className="font-black text-black block">⚠️ PERINGATAN REPLIKASI GERAKAN!</span> Ditemukan nomor punggung yang ganda di dalam antrean input di bawah. Mohon verifikasi kembali kesesuaian list baju! </div> </div> )} {/* Sizing Rows Editor Table */} <div className="overflow-x-auto border border-black rounded-2xl shadow-sm bg-white"> <table className="w-full text-sm text-center border-collapse"> <thead> <tr className="bg-black text-white font-black text-[10px] uppercase tracking-wider text-center"> <th className="p-3 w-12">No</th> <th className="p-3 w-24">No. Punggung</th> <th className="p-3 text-left">Nama Punggung (Backname)</th> <th className="p-3 text-left">Nama Dada (Depan)</th> <th className="p-3 w-36">Size</th> <th className="p-3 w-32">Lengan</th> <th className="p-3 w-28">Gender</th> <th className="p-3 text-left">Catatan Individu</th> <th className="p-3 w-12 rounded-tr-lg">Aksi</th> </tr> </thead> <tbody className="divide-y divide-slate-100"> {displayedSizingRows.map((row, idx) => ( <tr key={idx} className="hover:bg-black/5 font-bold"> <td className="p-2.5 text-center text-xs font-mono text-black">{idx + 1}</td> <td className="p-2.5"> <input type="text" value={row.no_player} onChange={(e) => handleSizingFieldChange(idx, 'no_player', e.target.value)} placeholder="-" className="w-full bg-white border border-black p-2 rounded-lg text-center text-xs font-black text-black" /> </td> <td className="p-2.5"> <input type="text" value={row.nama_punggung} onChange={(e) => handleSizingFieldChange(idx, 'nama_punggung', e.target.value.toUpperCase())} placeholder="NAMA PUNGGUNG" className="w-full bg-white border border-black p-2 rounded-lg text-xs font-bold uppercase text-black" /> </td> <td className="p-2.5"> <input type="text" value={row.nama_player} onChange={(e) => handleSizingFieldChange(idx, 'nama_player', e.target.value.toUpperCase())} placeholder="NAMA DEPAN" className="w-full bg-white border border-black p-2 rounded-lg text-xs font-bold uppercase text-black" /> </td> <td className="p-2.5"> <select value={row.size} onChange={(e) => handleSizingFieldChange(idx, 'size', e.target.value)} className="w-full bg-white border border-black p-2 rounded-lg text-xs font-black text-black" > {sizeOptions.map((sz) => ( <option key={sz} value={sz}> {sz} </option> ))} </select> </td> <td className="p-2.5"> <select value={row.lengan} onChange={(e) => handleSizingFieldChange(idx, 'lengan', e.target.value)} className="w-full bg-white border border-black p-2 rounded-lg text-xs font-bold" > <option value="PENDEK">PENDEK</option> <option value="PANJANG">PANJANG</option> </select> </td> <td className="p-2.5"> <select value={row.gender} onChange={(e) => handleSizingFieldChange(idx, 'gender', e.target.value)} className="w-full bg-white border border-black p-2 rounded-lg text-xs font-bold" > <option value="PRIA">PRIA</option> <option value="WANITA">WANITA</option> </select> </td> <td className="p-2.5"> <input type="text" value={row.keterangan} onChange={(e) => handleSizingFieldChange(idx, 'keterangan', e.target.value)} placeholder="Keterangan tambahan..." className="w-full bg-white border border-black p-2 rounded-lg text-xs font-medium text-black" /> </td> <td className="p-2.5"> <button type="button" onClick={() => handleRemoveRow(idx)} className="text-black hover:text-black font-extrabold text-base focus:outline-none" > × </button> </td> </tr> ))} {sizingRows.length === 0 && ( <tr> <td colSpan={9} className="p-8 text-center text-black italic"> Daftar rincian sizing jersey belum ditambahkan. Tekan 'Tambah Baris' untuk memulai inputan. </td> </tr> )} </tbody> </table> </div> </div> <button type="submit" className="w-full bg-gradient-to-r from-black to-black hover:from-black hover:to-black text-white p-4 font-black rounded-2xl text-xs uppercase tracking-widest shadow-xl flex items-center justify-center gap-2 transition-transform active:scale-95" > <Check className="w-4 h-4" /> SIMPAN SELURUH SPESIFIKASI & UKURAN SPK PRODUKSI KAKAMI </button> </form> </div> )} {/* MONITORING LIST SPK REGISTERS */} <div className="bg-white p-6 sm:p-8 rounded-3xl shadow-xl border border-black space-y-4"> <div className="flex flex-col sm:flex-row justify-between items-start sm:items-center gap-3"> <h2 className="text-base font-black text-black uppercase tracking-wider flex items-center gap-2"> 📊 Monitoring Berkas SPK Produksi ({bulan}) </h2> <div className="flex flex-col sm:flex-row gap-2 w-full sm:w-auto"> <div className="bg-white p-2 rounded-xl flex items-center gap-1.5 border border-black shadow-inner w-full sm:w-60"> <Search size={14} className="text-black" /> <input type="text" placeholder="Cari invoice atau nama..." value={searchSpk} onChange={(e) => setSearchSpk(e.target.value)} className="bg-transparent border-none outline-none text-xs text-black w-full placeholder-slate-400 font-semibold" /> </div> <div className="bg-white p-2 rounded-xl border border-black shadow-inner w-full sm:w-auto"> <select value={filterSpk} onChange={(e) => setFilterSpk(e.target.value)} className="bg-transparent border-none outline-none text-xs text-black font-semibold cursor-pointer px-1" > <option value="SEMUA" className="text-black">Semua Kategori</option> <option value="Jersey" className="text-black">Jersey</option> <option value="Kemeja" className="text-black">Kemeja</option> <option value="Jaket" className="text-black">Jaket</option> <option value="Kaos" className="text-black">Kaos</option> <option value="Polo" className="text-black">Polo</option> <option value="Training" className="text-black">Training</option> <option value="Lainnya" className="text-black">Lainnya</option> </select> </div> <div className="bg-white p-2 rounded-xl border border-black shadow-inner w-full sm:w-auto"> <select value={statusFilter} onChange={(e) => setStatusFilter(e.target.value)} className="bg-transparent border-none outline-none text-xs text-black font-semibold cursor-pointer px-1" > <option value="SEMUA" className="text-black">Semua Status (All)</option> <option value="AKTIF" className="text-black">Sembunyikan Selesai (On Progress)</option> <option value="SELESAI" className="text-black">Hanya Selesai (Done)</option> <option value="LENGKAP" className="text-black">Hanya Berkas Lengkap</option> <option value="KURANG" className="text-black">Hanya Berkas Kurang</option> </select> </div> </div> </div> <div className="overflow-x-auto"> <table className="w-full text-sm text-left border-collapse"> <thead> <tr className="bg-black text-white text-[10px] uppercase tracking-wider text-center"> <th className="p-4 w-28 rounded-tl-lg font-semibold">Tgl Order</th> <th className="p-4 text-left font-semibold">Nama Project & Pelanggan</th> <th className="p-4 w-32 font-semibold">Target Qty</th> <th className="p-4 w-40 font-semibold">List Terinput</th> <th className="p-4 w-48 font-semibold">Status Berkas</th> <th className="p-4 w-36 rounded-tr-lg font-semibold">Tindakan</th> </tr> </thead> <tbody className="divide-y divide-slate-100"> {filteredOrdersList.map((r) => { const target = r.jumlah_pcs; const terinput = orderItems.filter((i) => i.order_id === r.id).length; const isComplete = terinput >= target; return ( <tr key={r.id} className="hover:bg-black/5 text-center transition-colors"> <td className="p-4 text-xs font-mono text-black">{r.tanggal_order}</td> <td className="p-4 text-left"> <span className="text-[9px] font-black text-black block uppercase tracking-widest">{r.invoice_no}</span> <span className="font-extrabold text-black block text-xs uppercase">{r.nama_order}</span> <div className="text-[10px] text-black font-semibold mt-0.5">{r.nama_konsumen} ({r.no_telepon || '-'})</div> </td> <td className="p-4 text-black font-black text-xs">{target} Pcs</td> <td className="p-4 font-mono font-black text-xs rounded-xl"> <span className={`px-2.5 py-1.5 rounded-lg border inline-block min-w-[70px] ${isComplete ? 'bg-white text-black border-black' : 'bg-white text-black border-black'}`}> {terinput} / {target} </span> </td> <td className="p-4 text-center"> <div className="flex flex-col items-center gap-1"> {isComplete ? ( <span className="bg-white text-black border border-black rounded px-2.5 py-1 text-[9px] font-black tracking-widest uppercase block w-full text-center"> BERKAS LENGKAP </span> ) : ( <span className="bg-white text-black border border-black rounded px-2.5 py-1 text-[9px] font-black tracking-widest uppercase animate-pulse block w-full text-center"> RINCIAN INDIVIDU KURANG </span> )} <span className={`px-2 py-0.5 rounded text-[8px] font-black uppercase tracking-wider block border w-full text-center ${ r.status_tracking === "Selesai" ? "bg-black text-white border-black" : "bg-white text-black border-black/40" }`}> Tracking: {r.status_tracking || "DP"} </span> </div> </td> <td className="p-4"> <div className="flex items-center justify-center gap-2"> <button onClick={() => { setSelectedOrderId(r.id); loadSPKFieldsOfOrder(r, orderItems.filter((i) => i.order_id === r.id)); 
/* Scroll to form smoothly */
 document.getElementById('spk-form-box')?.scrollIntoView({ behavior: 'smooth' }); }} className="bg-black hover:bg-black text-white font-bold text-[10px] px-3 py-1.5 rounded-xl uppercase tracking-wider" > Isi SPK </button>
 <button onClick={() => handleOpenPrintObj(r)} className="border border-black text-black font-extrabold text-[10px] px-3 py-1.5 rounded-xl uppercase hover:bg-black/10 hover:text-white transition-colors" > SPK </button> </div> </td> </tr> ); })} {filteredOrdersList.length === 0 && ( <tr> <td colSpan={6} className="p-12 text-center text-black italic"> Belum ada orderan masuk di periode {bulan}. </td> </tr> )} </tbody> </table> </div> </div> {/* RENDER DETAILED HIGH-FIDELITY PRINT PREVIEW SPK DIALOGUE OVERLAY */} {showSpkPrintObj && ( <SpkPrintDocument order={showSpkPrintObj} allItems={orderItems} onClose={() => setShowSpkPrintObj(null)} /> )} </div> );
};
export default Spk;
