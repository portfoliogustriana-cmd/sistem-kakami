import React, { useState, useEffect } from "react";
import { useNavigate } from "react-router-dom";
import {
  Calendar,
  Users,
  ShoppingBag,
  Eye,
  CheckCircle2,
  ShieldAlert,
  AlertTriangle,
  Play,
  RefreshCw,
  X,
  Printer,
  Search,
  Settings,
  ArrowDownLeft,
  ArrowUpRight,
  Sparkles,
  MapPin,
  SearchCheck,
} from "lucide-react";
import {
  getStoredData,
  saveStoredData,
  Order,
  OrderItem,
  getStoredMonth,
  setStoredMonth,
} from "../lib/storage";
import { SpkPrintDocument } from "../components/SpkPrintDocument";
const Tracking = () => {
  const navigate = useNavigate();
  const [orders, setOrders] = useState<Order[]>([]);
  const [orderItems, setOrderItems] = useState<OrderItem[]>([]);
  const [bulan, setBulanState] = useState(getStoredMonth());
  const setBulan = (val: string) => {
    setBulanState(val);
    setStoredMonth(val);
  };
  const [statusAktif, setStatusAktif] = useState<string>(""); 
/* For filtering */
 const [searchTracking, setSearchTracking] = useState(''); 
/* Editing States */
 const [editingNotes, setEditingNotes] = useState<{ [key: number]: string }>({}); const [editingStatuses, setEditingStatuses] = useState<{ [key: number]: Order['status_tracking'] }>({}); 
/* SPK Print modal integration */
 const [spkPrintObj, setSpkPrintObj] = useState<Order | null>(null); const trackingStages: { id: Order['status_tracking']; name: string; percent: number; color: string; desc: string }[] = [ { id: 'DP', name: 'DP / Masuk', percent: 20, color: 'from-black to-black', desc: 'Down Payment diterima' }, { id: 'Produksi', name: 'Produksi', percent: 40, color: 'from-black to-black', desc: 'Proses potong & sablon' }, { id: 'Jait', name: 'Jait', percent: 60, color: 'from-black to-black', desc: 'Penjahitan kain & pemasangan rib' }, { id: 'Checking', name: 'Checking', percent: 80, color: 'from-black to-black', desc: 'Quality control & packing' }, { id: 'Selesai', name: 'Selesai', percent: 100, color: 'from-black to-black', desc: 'Tuntas & siap kirim' } ]; useEffect(() => { const data = getStoredData(); setOrders(data.orders); setOrderItems(data.orderItems); 
/* Initializing state maps */
 const notesMap: { [key: number]: string } = {}; const statusMap: { [key: number]: Order['status_tracking'] } = {}; data.orders.forEach((o) => { notesMap[o.id] = o.kendala_produksi || ''; statusMap[o.id] = o.status_tracking || 'DP'; }); setEditingNotes(notesMap); setEditingStatuses(statusMap); }, []); 
/* Recalculate monthly aggregates for visual buttons */
 const monthlyOrders = orders.filter((o) => o.tanggal_order.startsWith(bulan)); const status_counts = { DP: 0, Produksi: 0, Jait: 0, Checking: 0, Selesai: 0, }; monthlyOrders.forEach((o) => { const s = o.status_tracking || 'DP'; if (s in status_counts) { status_counts[s as keyof typeof status_counts]++; } }); 
/* Action update handler (Saves status & obstacle description) */
 const handleUpdateItem = (orderId: number) => { const targetStatus = editingStatuses[orderId] || 'DP'; const targetNote = editingNotes[orderId] || ''; const updated = orders.map((o) => { if (o.id === orderId) { return { ...o, status_tracking: targetStatus, kendala_produksi: targetNote, last_updated_status: new Date().toISOString().replace('T', ' ').substring(0, 16), }; } return o; }); setOrders(updated); const stored = getStoredData(); saveStoredData(updated, stored.orderItems, stored.transactions); alert('Informasi tahap tracking & kendala produksi berhasil disimpan!'); }; const handleFieldChange = (orderId: number, field: 'status' | 'note', val: string) => { if (field === 'status') { setEditingStatuses({ ...editingStatuses, [orderId]: val as Order['status_tracking'], }); } else { setEditingNotes({ ...editingNotes, [orderId]: val, }); } }; 
/* Filter current active table dataset */
 let tableData = monthlyOrders.filter((o) => o.nama_konsumen.toLowerCase().includes(searchTracking.toLowerCase()) || o.nama_order.toLowerCase().includes(searchTracking.toLowerCase()) || o.invoice_no.toLowerCase().includes(searchTracking.toLowerCase()) ); if (statusAktif) { tableData = tableData.filter((o) => (o.status_tracking || 'DP') === statusAktif); } tableData.sort((a, b) => b.id - a.id); 
/* DESC like PHP Model  */

/* Helper calculation for SLA dates */
 const calculateSLA = (o: Order) => { const deadline = o.deadline_kerja || o.tanggal_order; const today = new Date(); today.setHours(0, 0, 0, 0); const deadlineDate = new Date(deadline); deadlineDate.setHours(0, 0, 0, 0); const diffTime = deadlineDate.getTime() - today.getTime(); const diffDays = Math.ceil(diffTime / (1000 * 60 * 60 * 24)); return { days: diffDays, dateFormatted: new Date(deadline).toLocaleDateString('id-ID', { day: 'numeric', month: 'short', year: 'numeric' }), }; }; return ( <div className="p-4 sm:p-8 w-full max-w-7xl mx-auto flex flex-col gap-6"> {/* GLASSMORPHISM HEADER */} <div className="flex flex-col md:flex-row justify-between items-center bg-white/60 backdrop-blur-md p-6 rounded-3xl shadow-sm border border-black w-full"> <div> <h2 className="text-2xl font-black bg-clip-text text-transparent bg-gradient-to-r from-black to-black tracking-tight flex items-center gap-2"> <MapPin className="w-6 h-6 text-black" /> Monitor Produksi (Tracking) </h2> <p className="text-xs text-black font-medium mt-1"> Pantau pergerakan pesanan jersey & sandang secara real-time berdasarkan SLA pekerjaan. </p> </div> <div className="mt-4 md:mt-0 flex items-center gap-2 bg-white p-1.5 rounded-2xl border border-black shadow-inner"> <label className="font-extrabold text-[10px] text-black pl-2 uppercase tracking-wider">Periode Bulan:</label> <input type="month" value={bulan} onChange={(e) => setBulan(e.target.value)} className="bg-white hover:bg-white px-3 py-1.5 rounded-xl border-none outline-none text-xs font-bold text-black cursor-pointer transition-colors" /> </div> </div> {/* FILTER STATUS VISUAL BLOCKS */} <div className="space-y-3.5"> <div className="flex justify-between items-end px-1"> <h3 className="text-[10px] font-extrabold uppercase text-black tracking-widest flex items-center gap-1.5"> <SearchCheck size={13} /> Filter Status Visual </h3> {statusAktif && ( <button onClick={() => setStatusAktif('')} className="text-[9px] bg-white text-black border border-black px-3.5 py-1.5 rounded-full hover:bg-black hover:text-white transition-all font-black uppercase tracking-wider" > Reset Filter </button> )} </div> <div className="grid grid-cols-2 md:grid-cols-5 gap-4"> {trackingStages.map((stg) => { const isActive = statusAktif === stg.id; const count = status_counts[stg.id as keyof typeof status_counts] || 0; const boxBgs = { DP: 'bg-white dark:bg-black hover:bg-black/5 dark:hover:bg-white/10', Produksi: 'bg-white dark:bg-black hover:bg-black/5 dark:hover:bg-white/10', Jait: 'bg-white dark:bg-black hover:bg-black/5 dark:hover:bg-white/10', Checking: 'bg-white dark:bg-black hover:bg-black/5 dark:hover:bg-white/10', Selesai: 'bg-white dark:bg-black hover:bg-black/5 dark:hover:bg-white/10' }; const textColors = { DP: 'text-white dark:text-white', Produksi: 'text-black dark:text-white', Jait: 'text-black dark:text-white', Checking: 'text-black dark:text-white', Selesai: 'text-black dark:text-white' }; return ( <button key={stg.id} onClick={() => setStatusAktif(isActive ? '' : stg.id)} className={`text-center p-5 rounded-3xl border transition-all duration-300 flex flex-col items-center justify-center relative cursor-pointer ${ boxBgs[stg.id as keyof typeof boxBgs] } ${ isActive ? 'ring-2 ring-slate-900 scale-105 shadow-md border-black' : 'border-black hover:-translate-y-1' }`} > <span className={`text-base mb-1 font-black ${textColors[stg.id as keyof typeof textColors]}`}>{stg.name}</span> <span className="text-[9px] text-black font-bold tracking-wide uppercase leading-tight mb-2"> {stg.desc} </span> <span className={`text-3xl font-black ${textColors[stg.id as keyof typeof textColors]}`}>{count}</span> </button> ); })} </div> </div> {/* TRACKING PREMIUM WORK TABLE */} <div className="bg-white rounded-3xl shadow-xl border border-black overflow-hidden"> <div className="p-6 border-b border-black flex flex-col sm:flex-row justify-between items-start sm:items-center bg-white/40 gap-3"> <div> <h4 className="text-sm font-black text-black uppercase tracking-wider">📌 Logistik Order Terkini</h4> <p className="text-xs text-black font-medium"> {statusAktif === '' ? ( 'Menampilkan seluruh antrean tuntas dan tertunda periode ini.' ) : ( <span> Memfilter orderan tahapan:{' '} <strong className="text-black bg-white px-2 py-0.5 rounded-md border border-black text-[10px] uppercase"> {statusAktif} </strong> </span> )} </p> </div> <div className="bg-white p-2 rounded-xl flex items-center gap-1.5 border border-black shadow-inner w-full sm:w-60"> <Search size={14} className="text-black" /> <input type="text" placeholder="Cari invoice atau nama..." value={searchTracking} onChange={(e) => setSearchTracking(e.target.value)} className="bg-transparent border-none outline-none text-xs text-black w-full placeholder-slate-400 font-semibold" /> </div> </div> <div className="overflow-x-auto"> <table className="w-full text-sm text-left border-collapse"> <thead> <tr className="bg-black text-white text-[10px] uppercase tracking-widest text-center"> <th className="p-4 text-left w-64 rounded-tl-lg font-semibold">Informasi Project</th> <th className="p-4 w-44 font-semibold">SLA Targets</th> <th className="p-4 w-60 font-semibold">Live Progress</th> <th className="p-4 rounded-tr-lg font-semibold">Aksi & Kendala Produksi</th> </tr> </thead> <tbody className="divide-y divide-slate-100"> {tableData.map((row) => { const currentStatus = row.status_tracking || 'DP'; const activeStg = trackingStages.find((s) => s.id === currentStatus) || trackingStages[0]; const sla = calculateSLA(row); 
/* Determine badge style */
 let badgeNode = null; if (currentStatus === 'Selesai') { badgeNode = ( <span className="bg-white text-black border border-black px-3 py-1 rounded-full text-[9px] font-black tracking-widest uppercase shadow-sm"> ✓ TUNTAS </span> ); } else if (sla.days < 0) { badgeNode = ( <span className="bg-black text-white border border-black px-3 py-1 rounded-full text-[9px] font-black tracking-widest uppercase shadow-md shadow-sm animate-pulse block text-center"> ⚠ TELAT {Math.abs(sla.days)} HARI </span> ); } else if (sla.days <= 3) { badgeNode = ( <span className="bg-black text-white border border-black px-3 py-1 rounded-full text-[9px] font-black tracking-widest uppercase shadow-md shadow-sm block text-center"> ⚡ MEPET ({sla.days} Hari) </span> ); } else { badgeNode = ( <span className="bg-white text-black border border-black px-3 py-1 rounded-full text-[9px] font-black uppercase tracking-wider block text-center"> AMAN ({sla.days} Hari) </span> ); } return ( <tr key={row.id} className="hover:bg-white text-center transition-colors group align-top"> {/* INFO PROJECT */} <td className="p-4 text-left border-r border-black"> <span className="inline-block bg-white text-black px-2 py-0.5 rounded text-[9px] font-black uppercase tracking-widest mb-1 shadow-sm"> {row.invoice_no} </span> <span className="font-black text-black block text-xs uppercase group-hover:text-black transition-colors leading-snug"> {row.nama_order} </span> <div className="text-[10px] text-black font-bold mt-1 uppercase flex items-center gap-1"> <span className="w-1.5 h-1.5 rounded-full bg-white inline-block"></span> {row.nama_konsumen} </div> <div className="flex gap-2 mt-4.5"> <button onClick={() => setSpkPrintObj(row)} className="bg-white border border-black text-black font-black text-[9px] px-2.5 py-1.5 rounded-lg uppercase tracking-wider hover:bg-black hover:text-white transition-all flex items-center gap-1 shadow-sm" > <Eye size={12} /> Lihat SPK </button> <button onClick={() => navigate(`/spk?orderId=${row.id}`)} className="bg-white hover:bg-white text-black font-extrabold text-[9px] px-2.5 py-1.5 rounded-lg uppercase tracking-wider flex items-center" > Isi Sizing </button> </div> </td> {/* TARGETS & SLA */} <td className="p-4 border-r border-black"> <div className="bg-white/50 border border-black rounded-2xl p-3 inline-block w-full"> <div className="font-extrabold text-sm text-black"> {row.jumlah_pcs} <span className="text-[10px] font-medium text-black uppercase">Pcs</span> </div> <div className="h-px bg-white my-2"></div> <div className="text-[8.5px] text-black font-black block uppercase mb-1 tracking-wider">Tenggat Target:</div> <div className="text-[10px] font-extrabold text-black font-mono mb-2"> {sla.dateFormatted} </div> {badgeNode} </div> </td> {/* LIVE PROGRESS */} <td className="p-4 border-r border-black align-middle"> <div className="bg-white p-3.5 rounded-2xl border border-black shadow-inner"> <div className="w-full bg-white h-3.5 rounded-full overflow-hidden shadow-inner border border-black relative mb-2"> <div className={`h-full bg-gradient-to-r ${activeStg.color} rounded-full transition-all duration-700 flex items-center justify-end pr-1.5`} style={{ width: `${activeStg.percent}%` }} > {activeStg.percent > 20 && ( <span className="text-[8px] font-black text-white">{activeStg.percent}%</span> )} </div> </div> <div className="flex justify-between items-center text-[10px] font-black"> <span className="uppercase text-black tracking-wider"> {currentStatus} </span> <span className="text-[8.5px] text-black font-semibold uppercase font-mono"> Update: {row.last_updated_status ? row.last_updated_status : '-'} </span> </div> </div> </td> {/* AKSI & KENDALA FORM */} <td className="p-4"> <div className="flex flex-col gap-2 max-w-[340px] mx-auto"> <select value={editingStatuses[row.id] || 'DP'} onChange={(e) => handleFieldChange(row.id, 'status', e.target.value)} className="w-full bg-white border border-black text-black p-2.5 rounded-xl text-[10px] font-black uppercase outline-none focus:bg-white focus:ring-2 focus:ring-black cursor-pointer shadow-sm" > <option value="DP">📊 TAHAP 1: DP MASUK</option> <option value="Produksi">👕 TAHAP 2: PRODUKSI CUT/SABLON</option> <option value="Jait">🧵 TAHAP 3: JAIT TAYLOR</option> <option value="Checking">🔍 TAHAP 4: QC / CHECKING</option> <option value="Selesai">📦 TAHAP 5: SELESAI TUNTAS</option> </select> <textarea rows={2} value={editingNotes[row.id] || ''} onChange={(e) => handleFieldChange(row.id, 'note', e.target.value)} placeholder="Log kendala produksi jika ada..." className="w-full bg-white border border-black p-2.5 rounded-xl text-xs outline-none focus:bg-white text-black resize-none font-medium text-[11px]" /> <button onClick={() => handleUpdateItem(row.id)} className="bg-black text-white hover:bg-black/90 font-black text-[9px] uppercase tracking-widest py-2 rounded-xl transition-all shadow-md active:scale-95 flex items-center justify-center gap-1.5" > Simpan Status </button> </div> </td> </tr> ); })} {tableData.length === 0 && ( <tr> <td colSpan={4} className="p-12 text-center text-black italic"> Hebat! Tidak ada invoice pemesanan tertimbun di filter tahapan ini. </td> </tr> )} </tbody> </table> </div> </div> {spkPrintObj && ( <SpkPrintDocument order={spkPrintObj} allItems={orderItems} onClose={() => setSpkPrintObj(null)} /> )} </div> );
};
export default Tracking;
